function Marketing() {
    return (
        <div>
            this is marketing page with custom layout group
            layout group define using paranthesis in the path (layout-group) folder and add layout.tsx 
            we can define common layouts for shared pages
        </div>
    );
}

export default Marketing;