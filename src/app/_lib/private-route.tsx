function PrivatePage() {
    return (
        <div>
         private routes are used to restrict access to certain pages
         for seperating public and private routes
         public route define using _lib underscore folder
        </div>
    );
}

export default PrivatePage;