function Docs() {
    return (
        <div>
            this is catch all segment
            which means it will catch all the routes
            for example if you go to /docs/first
            it will catch that route
        </div>
    );
}

export default Docs;